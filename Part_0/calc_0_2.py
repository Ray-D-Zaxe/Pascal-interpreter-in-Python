# Lets take input as single line like we do in python
text = input("calc> ")

# Lets print the sum
print(int(text[0]) + int(text[2]))
# Note : We can only add 2 single digit numbers with no spaces in between !!!
# But thats alright for now